import React from "react";

const Tasks: React.FC = () => {
  return (
    <div>
      <form action="">
        <input type="text" />
        <button>Add </button>
      </form>
    </div>
  );
};

export default Tasks;
